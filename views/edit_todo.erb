<h1>Edit The Details of This Task</h1>
<p>Edit the information for this task below:</p>

<form action='/todos/edit/<%= @got_todo.first['id'] %>' method='post'>
  <input type="text" name="todo" value="<%= @got_todo.first['todo'] %>" size="30px"><br/>
  <input type="text" name="note" value="<%= @got_todo.first['note'] %>" size="30px"><br/>
  <input type="text" name="status" value="<%= @got_todo.first['status'] %>" size="30px"><br/>

  Who would you like to assign this task to?
  <select name="assigned_to">
    <% @got_people.each do |person| %>
      <option value="<%= person["id"] %>">
        <%= person["name"] %>
      </option>
    <% end %>
  </select><br>

  Which movie is this for?
  <select name="related_to">
    <% @got_movies.each do |movie| %>
      <option value="<%= movie["id"] %>">
        <%= movie["title"] %>
      </option>
    <% end %>
  </select><br>

  <button>Submit</button>
</form>




